
<h1>Title: <%= @review.title %></h1>
<h3>Restaurant name: <%= @review.restaurant.name %></h3>

<br>

<p>Read 👉: <%= @review.restaurant_review %></p>

<br>

<%= link_to "Return to Reviews Page", reviews_path %>

<br>
<br>

<% if @review.user.id == session[:user_id]%>
  <%= link_to "Edit", edit_review_path(@review) %>

  <br>
  
  <%= link_to 'Delete', @review, method: :delete, data: { confirm: 'For real? You can always rewrite your review at a later date.' } %>
<% end %>